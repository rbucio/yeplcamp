<% include partials/header %>

    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1><%= campground.name %></h1>
                <img src="<%= campground.image %>" alt="">
                <p><%= campground.desc %></p>
            </div>
        </div>
    </div>

<% include partials/footer %>
